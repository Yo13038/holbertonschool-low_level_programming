#include<stdio.h>
/**
* main - enter point
*
*print all single digit from 0 to 10
*Return: 0 (Succes)
*/
int main(void)
{
	int digit;
	for (digit = 0; digit < 10; digit++)
	{
		printf("%d", digit);
	}

	printf("\n");
	return (0);
}
